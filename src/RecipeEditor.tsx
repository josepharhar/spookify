import React from 'react';

class RecipeEditor extends React.Component {
  constructor(props: {}) {
    super(props);
  }

  render() {
    return (
      <div></div>
    );
  }
}

export default RecipeEditor;